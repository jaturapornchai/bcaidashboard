import { NextRequest, NextResponse } from 'next/server';

const OPENROUTER_API_KEY = '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';
const OPENROUTER_API_URL = 'https://openrouter.ai/api/v1/chat/completions';

export async function POST(request: NextRequest) {
  try {
    const { message, businessData, analysisType } = await request.json();

    // สร้าง prompt ตามประเภทการวิเคราะห์
    const getPromptByAnalysisType = (type: string, data: any) => {
      const dataSummary = JSON.stringify(data, null, 2);
      
      switch (type) {
        case 'sales':
          return `คุณเป็นที่ปรึกษาธุรกิจ SMEs ไทยที่เชี่ยวชาญ กรุณาวิเคราะห์ข้อมูลยอดขายต่อไปนี้:

${dataSummary}

กรุณาสร้างผลการวิเคราะห์ในรูปแบบ HTML ที่สวยงาม มีอิโจิ และแบ่งเป็น section ชัดเจน ให้คำแนะนำ:
1. 📈 แนวโน้มการขายเป็นอย่างไร
2. 🎯 มีช่วงไหนที่ขายดี/ขายไม่ดี
3. 💡 ความแนะนำในการปรับปรุง
4. 🚀 กลยุทธ์การขายที่เหมาะสม

ใช้ HTML tags สำหรับ formatting ตอบเป็นภาษาไทย เข้าใจง่าย สำหรับเจ้าของธุรกิจ SMEs`;

        case 'revenue':
          return `คุณเป็นที่ปรึกษาการเงินและธุรกิจ กรุณาวิเคราะห์ข้อมูลรายได้:

${dataSummary}

กรุณาสร้างผลการวิเคราะห์ในรูปแบบ HTML ที่สวยงาม มีอิโมจิ และแบ่งเป็น section ชัดเจน ให้คำแนะนำ:
1. 💰 การเติบโตของรายได้
2. 💵 การจัดการกระแสเงินสด
3. ⚠️ ความเสี่ยงทางการเงิน
4. 📈 กลยุทธ์การเพิ่มรายได้

ใช้ HTML tags สำหรับ formatting ตอบเป็นภาษาไทย เน้นปฏิบัติได้จริง`;

        case 'customers':
          return `คุณเป็นผู้เชี่ยวชาญด้าน Customer Experience กรุณาวิเคราะห์ข้อมูลลูกค้า:

${dataSummary}

กรุณาสร้างผลการวิเคราะห์ในรูปแบบ HTML ที่สวยงาม มีอิโมจิ และแบ่งเป็น section ชัดเจน ให้คำแนะนำ:
1. 👥 แนวโน้มลูกค้าใหม่
2. 🤝 การรักษาลูกค้า
3. 📢 กลยุทธ์การตลาด
4. ⭐ การปรับปรุงประสบการณ์ลูกค้า

ใช้ HTML tags สำหรับ formatting ตอบเป็นภาษาไทย เน้นลูกค้าเป็นศูนย์กลาง`;

        case 'products':
          return `คุณเป็นผู้เชี่ยวชาญด้าน Product Management กรุณาวิเคราะห์ข้อมูลสินค้า:

${dataSummary}

กรุณาสร้างผลการวิเคราะห์ในรูปแบบ HTML ที่สวยงาม มีอิโมจิ และแบ่งเป็น section ชัดเจน ให้คำแนะนำ:
1. 🏆 สินค้าขายดีและขายไม่ดี
2. 📦 กลยุทธ์การจัดการสินค้า
3. 🚀 การพัฒนาสินค้าใหม่
4. 💲 การกำหนดราคา

ใช้ HTML tags สำหรับ formatting ตอบเป็นภาษาไทย เน้นผลกำไร`;

        case 'regions':
          return `คุณเป็นผู้เชี่ยวชาญด้าน Market Analysis กรุณาวิเคราะห์ข้อมูลตามภาค:

${dataSummary}

กรุณาสร้างผลการวิเคราะห์ในรูปแบบ HTML ที่สวยงาม มีอิโมจิ และแบ่งเป็น section ชัดเจน ให้คำแนะนำ:
1. 🗺️ ภาคที่มีศักยภาพสูง
2. 🔧 ภาคที่ควรพัฒนา
3. 🌐 กลยุทธ์การขยายตลาด
4. 🎯 การปรับเป้าหมาย

ใช้ HTML tags สำหรับ formatting ตอบเป็นภาษาไทย เน้นขยายตลาด`;

        default:
          return `คุณเป็นที่ปรึกษาธุรกิจ SMEs ไทยที่เชี่ยวชาญ กรุณาวิเคราะห์ข้อมูลธุรกิจ:

${dataSummary}

${message}

กรุณาสร้างผลการวิเคราะห์ในรูปแบบ HTML ที่สวยงาม มีอิโมจิ ตอบเป็นภาษาไทย เข้าใจง่าย สำหรับเจ้าของธุรกิจ SMEs`;
      }
    };

    const prompt = getPromptByAnalysisType(analysisType, businessData);

    const response = await fetch(OPENROUTER_API_URL, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${OPENROUTER_API_KEY}`,
        'Content-Type': 'application/json',
        'HTTP-Referer': 'http://localhost:3000',
        'X-Title': 'SMEs Dashboard AI'
      },
      body: JSON.stringify({
        model: 'minimax/minimax-m2:free',
        messages: [
          {
            role: 'user',
            content: prompt
          }
        ],
        temperature: 0.7,
        max_tokens: 2000,
        top_p: 1,
        frequency_penalty: 0,
        presence_penalty: 0
      })
    });

    if (!response.ok) {
      throw new Error(`OpenRouter API error: ${response.status}`);
    }

    const data = await response.json();
    const aiResponse = data.choices[0]?.message?.content || 'ไม่สามารถวิเคราะห์ได้';

    return NextResponse.json({
      success: true,
      response: aiResponse,
      analysisType,
      timestamp: new Date().toISOString()
    });

  } catch (error) {
    console.error('AI Analysis Error:', error);
    return NextResponse.json(
      { 
        success: false, 
        error: 'ไม่สามารถวิเคราะห์ข้อมูลได้ กรุณาลองใหม่อีกครั้ง',
        details: error instanceof Error ? error.message : 'Unknown error'
      },
      { status: 500 }
    );
  }
}