# 🚀 BC AI Dashboard - แดชบอร์ดธุรกิจ SMEs ไทย พร้อม AI

แดชบอร์ดสำหรับติดตามผลการดำเนินงานธุรกิจ SMEs ที่ใช้ภาษาไทยและศัพท์ค้าขายที่เข้าใจง่าย พร้อม **AI ที่ปรึกษา** และ **เสียงพูดภาษาไทย**

## ✨ คุณสมบัติ

- 📊 แสดงข้อมูลการขายแบบ real-time
- 💰 ติดตามรายได้และกำไรรายเดือน
- 👥 วิเคราะห์ลูกค้าและช่องทางการขาย
- 📈 กราฟสวยงามแบบ interactive (ECharts)
- 🤖 **AI ที่ปรึกษาธุรกิจ** - วิเคราะห์ข้อมูลและให้คำแนะนำ
- 🔊 **Text-to-Speech ภาษาไทย** - อ่านผลการวิเคราะห์
- 🔄 รองรับ data caching และ streaming
- 🇹🇭 ใช้ พ.ศ. และภาษาไทย

## 🛠️ เทคโนโลยีที่ใช้

### Frontend
- **Next.js 14** - Framework สำหรับ React (App Router)
- **shadcn/ui** - UI Components สวยงาม
- **Apache ECharts** - กราฟขั้นสูงที่รองรับภาษาไทย
- **Tailwind CSS** - Styling
- **TypeScript** - Type Safety

### AI & APIs
- **OpenRouter API** - AI Analysis (MiniMax Model)
- **ElevenLabs API** - Text-to-Speech เสียงไทย

### Security
- **Zod** - Schema Validation
- Rate Limiting
- API Key Encryption

## 🚀 การติดตั้ง

1. **Clone repository**
```bash
git clone https://github.com/jaturapornchai/bcaidashboard.git
cd bcaidashboard
```

2. **ติดตั้ง dependencies**
```bash
npm install
```

3. **ตั้งค่า Environment Variables**
```bash
# คัดลอกจาก template
cp .env.example .env.local

# แก้ไข .env.local ให้ครบทุกค่า
# - OPENROUTER_API_KEY
# - ELEVENLABS_API_KEY
# - DASHBOARD_API_SECRET
# - API_KEY_ENCRYPTION_KEY
```

4. **รัน development server**
```bash
npm run dev
```

5. **เปิด browser**
```
http://localhost:3000
```

## 📦 Production Deployment

ดูคู่มือการ Deploy แบบละเอียดใน **[DEPLOYMENT.md](./DEPLOYMENT.md)**

### Quick Deploy to Vercel:
```bash
npm install -g vercel
vercel --prod
```

อย่าลืมตั้งค่า Environment Variables ใน Vercel Dashboard!

## โครงสร้างโปรเจค

```
├── app/
│   ├── api/dashboard/route.ts    # API endpoint
│   ├── layout.tsx               # Root layout
│   ├── page.tsx                 # Main dashboard
│   └── globals.css              # Global styles
├── components/
│   ├── ui/                      # shadcn/ui components
│   └── charts/                  # Chart components
├── data/
│   └── mockData.ts              # Mock data สำหรับ SMEs
├── hooks/
│   └── useDashboardData.ts      # Custom hooks
└── lib/
    └── utils.ts                 # Utility functions
```

## การใช้งาน

### Dashboard Features

1. **สถิติหลัก** - ยอดขาย, รายได้, ลูกค้า, สินค้าขายดี
2. **กราฟยอดขาย** - แนวโน้มการขายรายเดือน
3. **กราฟรายได้** - รายได้แยกตามเดือน
4. **ช่องทางขาย** - การกระจายยอดขายตามช่องทาง
5. **สินค้าขายดี** - สินค้าที่ขายดีที่สุด
6. **ยอดขายตามภาค** - เปรียบเทียบยอดขายกับเป้าหมาย
7. **ลูกค้าใหม่** - จำนวนลูกค้าใหม่รายเดือน

### การเปลี่ยนแปลงข้อมูล

สามารถแก้ไขข้อมูล mock ใน `data/mockData.ts` เพื่อใช้กับข้อมูลจริงของคุณ

### การปรับแต่ง UI

- แก้ไขธีมใน `app/globals.css`
- เปลี่ยนสีใน `tailwind.config.js`
- เพิ่ม components ใหม่ใน `components/ui/`

## การพัฒนาต่อ

### เพิ่มกราฟใหม่

1. สร้าง component ใน `components/charts/`
2. เพิ่มใน dashboard page
3. อัปเดต API หากต้องการข้อมูลใหม่

### เชื่อมต่อฐานข้อมูลจริง

1. แทนที่ mock data ด้วย API calls
2. อัปเดต API route ใน `app/api/dashboard/route.ts`
3. เพิ่ม authentication หากจำเป็น

### เพิ่มฟีเจอร์ใหม่

- การรายงาน (Reports)
- การส่งออกข้อมูล (Export)
- การแจ้งเตือน (Notifications)
- การตั้งค่าเป้าหมาย (Goals Setting)

## License

MIT License

## Support

หากมีปัญหาหรือต้องการความช่วยเหลือ กรุณาสร้าง issue ใน repository